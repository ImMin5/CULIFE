<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.culife.dao.AdminDAO">
	<insert id="reviewWrite">
		insert into review(no, member_no, content) values(#{no}, #{member_no}, #{content})
	</insert>
	<select id="reviewList" resultType="ReviewVO">
		select no, member_no, content, writedate from review
		where title=${param1}
		order by no desc
	</select>
	<update id="reviewEdit">
		update review set content=#{content} where no=${no} and member_no=#{member_no}
	</update>
	<delete id="reviewDel">
		delete from review where no=${param1} and member_no=#{param2}
	</delete>
</mapper>